fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
alembic==1.13.0
python-jose[cryptography]==3.3.0
pydantic==2.9.0
pydantic-settings==2.5.0
python-multipart==0.0.9
aiofiles==24.1.0
pyyaml==6.0.2
httpx==0.27.0
watchdog==5.0.0
cryptography>=42.0.0

# 密码哈希 - 优先用 bcrypt(快)，失败则用 passlib(纯Python)
# Windows/ARM 安装 bcrypt 失败时会自动降级
bcrypt>=4.0.0

# 核心模块编译 (安装脚本会自动编译 .pyx → .so/.pyd)
# cython>=3.0.0  # 由安装脚本安装，不放在这里避免用户手动漏编译

# 语义搜索 (可选，安装失败不影响基础功能)
chromadb>=0.4.0

# 图算法 (可选，用于知识图谱分析)
networkx>=3.0

# 定时任务 (用于日报等定时功能)
apscheduler>=3.10.0
